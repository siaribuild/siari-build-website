# API routes are handled by Pages Functions — never rewrite them.
# (Functions already take precedence, this line documents the intent.)
/api/*  /api/:splat  200

# SPA fallback: any other route serves index.html so client-side routing works.
# 200 = rewrite (URL stays the same), not a redirect.
/*  /index.html  200
